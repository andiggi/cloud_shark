# Email address to receive cost reports
EMAIL_ADDRESS=your-email@example.com

# Cron schedule for the report (default: daily at 9 AM UTC)
# Format: cron(minutes hours day-of-month month day-of-week year)
# Examples:
# Daily at 9 AM UTC: cron(0 9 * * ? *)
# Every Monday at 8 AM UTC: cron(0 8 ? * MON *)
# First day of month at 10 AM UTC: cron(0 10 1 * ? *)
REPORT_SCHEDULE=cron(0 9 * * ? *)

# AWS Configuration (optional - will use default AWS credentials)
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key